package {{PACKAGE}}.desktop

import sgl._
import sgl.awt._
import sgl.awt.util._

import sgl.analytics.LoggedAnalyticsProvider
import sgl.ads.NoAdsProvider
import sgl.util.metrics.DefaultInstrumentationProvider

object {{MAIN_CLASS}} extends AWTApp with {{CORE_ABSTRACT_CLASS}}
  with VerboseStdErrLoggingProvider
  with LiftJsonProvider
  with GameLoopStatisticsComponent
  with DefaultInstrumentationProvider
  with NoAdsProvider
  with LoggedAnalyticsProvider
  with {{SAVE_COMPONENT}}
{
  override val TargetFps: Option[Int] = Some(60)

  override val frameDimension = ({{FRAME_WIDTH}}, {{FRAME_HEIGHT}})

  override val DynamicResourcesEnabled = true

  override val ScreenForcePPI: Option[Float] = Some(160f)

  {{SAVE_COMPONENT_INIT}}
}
